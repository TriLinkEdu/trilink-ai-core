fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0

# Embedders & Re-rankers
# sentence-transformers provides both:
#   - all-MiniLM-L6-v2  (bi-encoder, used by MiniLMEmbedder for dense retrieval)
#   - cross-encoder/ms-marco-MiniLM-L-6-v2  (cross-encoder, used by ChatService re-ranker)
sentence-transformers==3.0.1

# Sparse (BM25) retrieval — used by Advanced RAG hybrid search
rank-bm25==0.2.2

# LLM providers
groq==0.9.0
openai>=1.40.0          # ≥1.40 required for beta.chat.completions.parse (Structured Outputs)
anthropic>=0.20.0
google-generativeai>=0.8.0  # ≥0.8 required for response_schema Structured Outputs

# Database
psycopg2-binary==2.9.9
pgvector==0.3.2
pymongo==4.8.0

# Utilities
certifi>=2024.0.0
pdfplumber>=0.10.0
numpy==1.26.4
requests>=2.31.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0
